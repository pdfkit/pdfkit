language: ruby

rvm:
- 2.3
- 2.4
- 2.5
- 2.6

before_install:
  - gem update --system
  - gem update bundler

before_script:
  - "sudo apt-get -qq -y install fontconfig libxrender1"
  - "wget hhttps://github.com/wkhtmltopdf/packaging/releases/download/0.12.6-1/wkhtmltox_0.12.6-1.buster_amd64.deb"
  - "sudo apt-get install ./wkhtmltox_0.12.6-1.buster_amd64.deb"
